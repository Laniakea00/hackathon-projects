.idea
.venv
venv/
.env

# large data files (corpus too big for git — share separately)
data/raw_protocols/
data/protocols_corpus.jsonl

# evaluation results (generated, not source)
data/evals/
# embedding cache (large .npy files, regenerated by bootstrap)
data/.embeddings/

# pre-downloaded uv Linux binary for offline Docker builds
/uv
/uv-linux.tar.gz

# Python cache
__pycache__/
*.pyc
*.pyo